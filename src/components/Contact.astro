<section
  id="contact"
  class="pt-32 pb-24 px-6 md:px-12 bg-gradient-to-b from-white to-orange-50 text-center"
>
  <h2 class="text-3xl md:text-4xl font-bold mb-4 text-gray-800 tracking-tight">
    Contact Us
  </h2>
  <p class="text-lg text-gray-600 max-w-xl mx-auto mb-12 leading-relaxed">
    We'd love to hear from you. Have questions, feedback, or just want to
    connect? Send us a message and we’ll get back to you soon.
  </p>
  <form
    action="https://formspree.io/f/movdpgre"
    method="POST"
    class="max-w-2xl mx-auto text-left bg-white p-8 md:p-10 rounded-2xl shadow-xl border border-orange-100"
  >
    <!-- Email -->
    <div class="mb-6">
      <label for="email" class="block text-sm font-semibold text-gray-700 mb-2"
        >Your email</label
      >
      <input
        type="email"
        id="email"
        name="email"
        required
        placeholder="you@example.com"
        class="w-full px-4 py-3 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-orange-400 transition-all"
      />
    </div>

    <!-- Message -->
    <div class="mb-6">
      <label
        for="message"
        class="block text-sm font-semibold text-gray-700 mb-2"
        >Your message</label
      >
      <textarea
        id="message"
        name="message"
        rows="5"
        required
        placeholder="Write your message here..."
        class="w-full px-4 py-3 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-orange-400 transition-all"
      ></textarea>
    </div>

    <!-- Submit Button -->
    <div class="text-center md:text-left">
      <button
        type="submit"
        class="w-full md:w-auto bg-orange-500 text-white font-semibold px-8 py-3 rounded-xl hover:bg-orange-600 transition-colors duration-300 shadow-md hover:shadow-lg"
      >
        Send
      </button>
    </div>
  </form>
</section>
